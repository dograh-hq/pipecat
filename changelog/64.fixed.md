- Fixed Whisper-based STT services (`OpenAISTTService`, `GroqSTTService`, ...) dropping the turn with an error when an OpenAI-compatible server returns `"text": null`; it is now handled as an empty transcription.
