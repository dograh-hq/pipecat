- MiniMax HTTP TTS retries transient failures before audio begins, with bounded attempts, a configurable first-audio deadline, and provider retry timing. Pending audio contexts remain active during retries, and failed attempts do not record usage.
