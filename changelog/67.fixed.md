- MiniMax HTTP TTS retries transient failures before audio begins, with bounded attempts, a configurable first-audio deadline, and provider retry timing. Pending audio contexts remain active during retries, and attempts that produce no audio do not record usage.
